# Secrets — never commit real keys
.env

# Dependencies / junk
node_modules/
*.log
.DS_Store

# Review artifacts — regenerable via `node tools/shots.mjs`
/shots/

# Scratch analysis scripts and crops from review agents
/_*.mjs
/_crops/
